<nav>
  <div class="nav-wrapper">
    <a href="#" class="brand-logo">Logo</a>
    <ul id="nav-mobile" class="right hide-on-med-and-down">
      <li><%= @cart.pieces %>x, <%= eur @cart.price %></li>
      <li><%= link_to (t :Products), products_path %></li>
      <li><%= link_to (t :sign_in), new_user_session_url %></li>
      <li><%= link_to ('admin'), root_path if admin_signed_in? %></li>
      <% if user_signed_in? %>
        <li><span><%= current_user.email %></span></li>
      <% else %>
        <li><%= link_to (t :sign_in), new_user_session_url %></li>
      <% end %>
      <% if user_signed_in? %>
        <li><%= link_to (t :Profile), edit_user_registration_url %></li>
        <li><%= link_to (t :sign_out), destroy_user_session_url, data: {method: :delete} %></li>
      <% end %>
    </ul>
  </div>
</nav>